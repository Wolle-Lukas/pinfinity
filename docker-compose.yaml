services:
  pinfinity:
    container_name: pinfinity
    # Build the image locally from the repository Dockerfile. Without this
    # Compose will try to pull an image named "pinfinity" from a registry
    # which results in "pull access denied" if it doesn't exist or is private.
    build: .
    image: pinfinity:latest
    environment:
      PINFINITY_NAME: "XXX"
      PINFINITY_EMAIL: "XXX"
      PINFINITY_DEVICE_ID: "XXX"
      PINFINITY_DEVICE_NAME: "XXX"
      PINFINITY_SERIAL_NUMBER: "XXX"
    restart: always
    networks:
      - web
    volumes:
      - ./pinfinity:/app/app/data

  nginx:
    image: nginx:alpine
    container_name: nginx-proxy
    restart: always
    networks:
      - web
    depends_on:
      - pinfinity
    ports:
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./certs:/etc/nginx/certs:ro

networks:
  web:
    driver: bridge
    
